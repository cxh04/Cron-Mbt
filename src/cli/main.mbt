fn main {
  println("Cron-Mbt CLI v0.1.0")
  println("Use this tool to parse and debug cron expressions locally.")
  // Demonstration logic
  // let expr = @cron.parse("*/5 * * * *").unwrap()
  // println(expr)
}
