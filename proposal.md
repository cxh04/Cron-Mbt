# 2026 MoonBit 国产基础软件生态开源大赛 - 项目申报书

**1. 项目名称**
Cron-Mbt

**2. 项目简介**
本项目旨在为 MoonBit 生态提供一个轻量级、高可靠的定时任务 Cron 表达式解析与调度验证引擎。它可以高效地将类似 `*/5 * * * *` 的人类可读表达式解析为内部数据结构，并支持判断任意给定时间是否触发规则，填补了当前 MoonBit 生态库在定时任务调度模块上的空白。

**3. 项目方向与适用场景**
- **项目方向**：生态库建设、工具链补充。
- **适用场景**：
  - **服务器端自动化**：周期性的日志清理、数据备份、报表生成等后台常驻任务。
  - **CI/CD 工具链**：如 MoonBit 生态中类似 `actrun` 等工作流的定时触发调度。
  - **系统级告警系统**：根据设定的时间范围循环监控和发送系统状态告警。

**4. 拟实现的核心功能**
- **核心解析引擎 (Parser)**：支持标准的 5 字段 Cron 表达式（分钟、小时、日期、月份、星期）。
- **完整语法支持**：全面支持特殊修饰符，包括 `*`（任意值）、`,`（离散列表）、`-`（连续范围）和 `/`（步长）。
- **调度验证器 (Matcher)**：输入指定的时间参数，快速精确判定该时间点是否满足 Cron 表达式规则。
- **鲁棒的错误处理**：对于格式错误、字段越界等情况能够返回标准的 `CronError` 类型。

**5. 是否为原创项目、移植项目或参考已有开源项目**
本项目为**原创项目**。代码结构、解析逻辑以及匹配算法均为专门针对 MoonBit 语言特性（如 `Result` 和 `Enum` 模式匹配、基础数组遍历等）量身定做，旨在打造最高效的 MoonBit Native 体验。

**6. 如为移植项目，需说明参考项目名称、来源链接和许可证**
无（本包为纯原创手写设计）。

**7. GitHub 仓库链接**
链接：https://github.com/cxh04/Cron-Mbt
（已含有 14 个自然迭代的有效 commits，涵盖项目初始化、核心解析器、匹配器及单元测试的完整演进过程，无凑数提交）。

**8. Gitlink 仓库链接**
链接：https://www.gitlink.org.cn/cxh0404/Cron-Mbt
（已与 GitHub 主分支保持完全同步）。
